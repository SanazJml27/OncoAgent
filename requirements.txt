# Core dependencies -- everything needed to run the pipeline fully offline.
pydantic>=2.0
numpy>=1.24
scikit-learn>=1.3
pytest>=7.4
streamlit>=1.35

# --- Optional extras (uncomment / pip install separately as needed) ---
#
# Real Hugging Face NER-based redaction (ONCOAGENT_USE_HF_NER=1):
#   transformers>=4.40
#   torch>=2.2
#
# Real Hugging Face sentence-embedding retrieval (ONCOAGENT_USE_HF_EMBEDDINGS=1):
#   sentence-transformers>=2.6
#
# Real LLM-generated summaries instead of the deterministic template:
#   anthropic>=0.34   # set ANTHROPIC_API_KEY
#   openai>=1.30      # set OPENAI_API_KEY
